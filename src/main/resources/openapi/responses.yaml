NotFound:
  description: Not Found
  content:
    application/json:
      schema:
        $ref: './entities.yaml#/NotFound'

ApiKeyError:
  description: Api key error
  content:
    application/json:
      schema:
        $ref: './entities.yaml#/ApiKeyError'

WrongCityError:
  description: Wrong city error
  content:
    application/json:
      schema:
        $ref: './entities.yaml#/WrongCityError'

LimitError:
  description: Request limit
  content:
    application/json:
      schema:
        $ref: './entities.yaml#/LimitError'


BadRequest:
  description: Not valid Request
  content:
    application/json:
      schema:
        $ref: './entities.yaml#/BadRequest'

GenericError:
  description: Generic Error
  content:
    application/json:
      schema:
        $ref: './entities.yaml#/GenericError'

